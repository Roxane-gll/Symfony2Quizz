{% extends 'base.html.twig' %}

{% block body %}
    <div class="formParticipate">
        <div class="exportListeCsv">
            <a href="{{ path('app_quizz_export') }}">Exporter la liste de Quiz en csv</a>
        </div>
        <div class="form">
            <p>
            <div class="formPArticipate-sub">
                <p>
                    Liste des quiz non remplis
                </p>
                {{ otherQuizzs|length }}
                <ul>
                    {% for quizz in otherQuizzs %}
                        <li><a href="/quizz/{{ quizz.id }}">{{ quizz.title }}</a></li>
                    {% endfor %}
                </ul>
            </div>

            <div class="formPArticipate-sub">
                <p>
                    Liste des quiz déjà faits
                </p>
                {{ userQuizzs|length }}
                <ul>
                    {% for quizz in userQuizzs %}
                        <li><a href="/quizz/{{ quizz.id }}">{{ quizz.title }}</a></li>
                    {% endfor %}
                </ul>
            </div>

            <div class="formPArticipate-sub">
                <p>
                    Liste des quiz que j'ai créés
                </p>
                {{ authoredQuizzs|length }}
                <ul>
                    {% for quizz in authoredQuizzs %}
                        <li><a href="/quizz/{{ quizz.id }}">{{ quizz.title }}</a></li>
                    {% endfor %}
                </ul>
            </div>
            </p>
        </div>
    </div>


{% endblock %}